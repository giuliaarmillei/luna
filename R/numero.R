# funcione if else
numero <- function(x){
  if(x>0) {
    "Questo numero è positivo"
    }
  else if(x<0) {
    "Questo numero è negativo"
    }
  else {
    "Zero non è negativo nè positivo"
    }
  }
